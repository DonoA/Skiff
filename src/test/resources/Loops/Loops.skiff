def main(List<String> args) -> Int {
    let Int i = 0;
    
    while(i < 2 && true) {
        println("while");
        i++;
    }

    let Int j = 4;

    loop {
        println("loop");
        if(j < 1) {
            break;
        }
        j--;
    }

    for(let Int k = 0; k < 2; k++) {
        println("bonk");
    }

    return i + j;
}