def main(args: List<String>): Int {
    i: Int = 0;
    
    while(i < 2 && true) {
        println("while");
        i++;
    }

    j: Int = 4;

    loop {
        println("loop");
        if(j < 1) {
            break;
        }
        j--;
    }

    for(i: Int = 0; i < 2; i++) {
        println("bonk");
    }

    return i + j;
}