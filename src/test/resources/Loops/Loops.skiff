def main(args: List<String>): Int {
    i: Int = 0;
    
    while(i < 2 && true) {
        println("while");
        i++;
    }

    j: Int = 4;

    loop {
        println("loop");
        if(j < 1) {
            break;
        }
        j--;
    }

    for(k: Int = 0; k < 2; k++) {
        println("bonk");
    }

    return i + j;
}