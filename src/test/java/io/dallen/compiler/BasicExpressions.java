package io.dallen.compiler;

import io.dallen.AST;

import static org.junit.Assert.assertEquals;

public class BasicExpressions {

    @org.junit.Test
    public void parseAssign() {
//        CompileContext context = new CompileContext(null);
//
//        context.declareObject(new CompiledVar("x", CompiledType.INT));
//
//        AST.Statement toTest =  new AST.Assign(new AST.Variable("x"), new AST.NumberLiteral(5.0));
//        CompiledCode code = toTest.compile(context);
//
//        String expected = "";
//
//        assertEquals(expected, code.getCompiledText());
    }
}
